import 'package:flutter/material.dart';

class CurrentAppTheme {
  static ThemeData defaultTheme() {
    return ThemeData(
      colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFFCB8FD5))
    );
  }
}
